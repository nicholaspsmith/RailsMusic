<div class="row selecters">
	<div class="large-4 columns key-selecter">
		<label>Key</label>
		<select name="mydropdown" id="key-dropdown">
			<!-- Populate through jquery instead -->
		</select>
	</div>
	<div class="large-4 columns scale-select">
		<label>Scale</label>
		<select name="mydropdown" id="scale-dropdown">
			<!-- Populate through jquery instead -->
		</select>
	</div>
</div>

<div class="body">
	<h3 id="key">A</h3>
	<h3 id="scale">major</h3>
	<div id="notes"></div>
</div>
